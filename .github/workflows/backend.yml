name: Deploy Backend

on:
  push:
    branches:
      - 'master'
    paths:
      - 'backend/**'


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the backend directory
        run: |
          echo 'deploying to python any where'